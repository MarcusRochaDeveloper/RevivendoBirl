{
    "version": "0.2.0",
    "configurations": [
        {
            "name": "Lançar Extensão (BIRL Monorepo)",
            "type": "extensionHost",
            "request": "launch",
            "runtimeExecutable": "${execPath}",
            "args": [
                "--extensionDevelopmentPath=${workspaceFolder}"
            ],
            "outFiles": [
                "${workspaceFolder}/../birl-language-server/**/*.js"
            ]
        }
    ]
}
